From f3a868e9fe32b69834b22099bef944992fc511bc Mon Sep 17 00:00:00 2001
From: sgateway <299698546+sgateway@users.noreply.github.com>
Date: Tue, 4 Aug 2026 14:53:42 -0700
Subject: [PATCH 4/7] Adapt Windows launch trust to current upstream

---
 src/install.ts                    |  6 ++++--
 tests/defenseclaw-runtime.test.ts | 20 ++++++++++++++++----
 2 files changed, 20 insertions(+), 6 deletions(-)

diff --git a/src/install.ts b/src/install.ts
index e9e50c06ad942bf0be143a98122c5e5f8dff369d..ea3fa1cf649349566a399d7c11787bd3334954a4 100644
--- a/src/install.ts
+++ b/src/install.ts
@@ -837,7 +837,8 @@ export function stopWindowsSurfaces(options: WindowsSurfaceScope = {}): WindowsS
       SGW_STOP_PORT: String(endpoint.port),
       SGW_STOP_POWERSHELL_PATH: powershell
     }),
-    stdio: ["ignore", "pipe", "pipe"]
+    stdio: ["ignore", "pipe", "pipe"],
+    windowsHide: true
   });
   if (result.status !== 0) {
     throw new Error(result.stderr.trim() || "Could not stop the running s-gw Windows surfaces.");
@@ -1668,7 +1669,8 @@ export async function openWindowsClient(options: MenuBarOptions = {}): Promise<W
       cwd: path.dirname(powershell),
       encoding: "utf8",
       env: windowsEnvironment(url),
-      stdio: ["ignore", "pipe", "pipe"]
+      stdio: ["ignore", "pipe", "pipe"],
+      windowsHide: true
     }
   );
 
diff --git a/tests/defenseclaw-runtime.test.ts b/tests/defenseclaw-runtime.test.ts
index a1b5fb6490a051f48fc4a8507b32888804d2194f..b8ea5fd9a6d7eac3b6fd94d6fa55afb79e2b021c 100644
--- a/tests/defenseclaw-runtime.test.ts
+++ b/tests/defenseclaw-runtime.test.ts
@@ -6,7 +6,7 @@ import { pathToFileURL } from "node:url";
 import { describe, expect, it } from "vitest";
 
 const repoRoot = process.cwd();
-const tsxBin = path.join(repoRoot, "node_modules", ".bin", process.platform === "win32" ? "tsx.cmd" : "tsx");
+const tsxCli = path.join(repoRoot, "node_modules", "tsx", "dist", "cli.mjs");
 
 describe("DefenseClaw local console launch", () => {
   it("admits only the host-resolved PowerShell path on Windows", async () => {
@@ -23,6 +23,18 @@ describe("DefenseClaw local console launch", () => {
     expect(trusted).toContain("realpathSync(candidate)");
   });
 
+  it("hides every synchronous PowerShell window", async () => {
+    const install = await readFile(new URL("../src/install.ts", import.meta.url), "utf8");
+    const starts = [...install.matchAll(/spawnSync\(\s*powershell,/g)];
+    const calls = [...install.matchAll(/spawnSync\(\s*powershell,[\s\S]*?\n\s*\}\s*\);/g)];
+
+    expect(calls.length).toBeGreaterThan(0);
+    expect(calls.length).toBe(starts.length);
+    for (const call of calls) {
+      expect(call[0]).toContain("windowsHide: true");
+    }
+  });
+
   it("contains no TypeScript proxy-tokenizer or approval mutation fallback", async () => {
     const cli = await readFile(new URL("../src/cli.ts", import.meta.url), "utf8");
     const mcp = await readFile(new URL("../src/mcp-server.ts", import.meta.url), "utf8");
@@ -162,7 +174,7 @@ describe("DefenseClaw local console launch", () => {
       const recoveryHome = path.join(tmpDir, "recovery");
 
       try {
-        const result = spawnSync(tsxBin, [path.join(repoRoot, "src", "cli.ts"), ...route], {
+        const result = spawnSync(process.execPath, [tsxCli, path.join(repoRoot, "src", "cli.ts"), ...route], {
           cwd: repoRoot,
           encoding: "utf8",
           env: {
@@ -208,7 +220,7 @@ describe("DefenseClaw local console launch", () => {
       const recoveryHome = path.join(tmpDir, "recovery");
 
       try {
-        const result = spawnSync(tsxBin, [path.join(repoRoot, "src", "cli.ts"), ...route], {
+        const result = spawnSync(process.execPath, [tsxCli, path.join(repoRoot, "src", "cli.ts"), ...route], {
           cwd: repoRoot,
           encoding: "utf8",
           env: {
@@ -257,7 +269,7 @@ describe("DefenseClaw local console launch", () => {
       await writeFile(helperPath, helper, { mode: 0o700 });
       await chmod(helperPath, 0o700);
       await writeFile(wrapper, wrapperSource);
-      const output = execFileSync(tsxBin, [wrapper], {
+      const output = execFileSync(process.execPath, [tsxCli, wrapper], {
         cwd: repoRoot,
         encoding: "utf8",
         env: {
